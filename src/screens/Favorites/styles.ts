import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  listEmptyText: {
    color: '#ccc',
    fontSize: 18,
    textAlign: 'center'
  },
  title: {
    color: '#d8d8d8',
    marginBottom: 20,
    paddingBottom: 15,
    fontSize: 25,
    textAlign: 'center',
    borderBottomColor: '#535353',
    borderBottomWidth: 1
  }
})